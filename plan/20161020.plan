neo4j locally

create a node in neo4j

create a node in neo4j with lat/long properties.

google maps on the page , one marker

get properties of one node in neo via plain old js

marker location from neo4j via javascript


dockerize that stuff.
nginx neo4j
popoto.js, connect to neo4j for 3 nodes and one edge
spatial functions for neo4j
s2
postgresql bottled water

DDproj
